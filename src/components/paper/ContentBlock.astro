---
const { id, layout = 'vertical' } = Astro.props;
---

<section id={id} class={`parsed-block py-6 border-b border-gray-200 dark:border-gray-700 ${layout === 'side-by-side' ? 'md:grid md:grid-cols-2 md:gap-6' : 'space-y-4'}`}>
  <slot />
</section>
